all:
  children:
    ubuntu:
      hosts:
        ubuntu-server:
          ansible_host: 10.0.129.5
          ansible_user: admin
          ansible_password: admin
          ansible_become: true
          ansible_become_pass: admin
    centos:
      hosts:
        centos-server:
          ansible_host: 10.0.129.6
          ansible_user: admin
          ansible_password: admin
          ansible_become: true
          ansible_become_pass: admin
